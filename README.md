# TravelRequests — Scaffold

Initial scaffold for the TravelRequests Clean Architecture solution.

Next steps:

- Run `dotnet new sln` and add projects, or use the provided csproj files under `src/`.
